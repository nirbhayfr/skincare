function IconBg({ children }) {
     return (
          <div className="flex items-center justify-center bg-primary-light rounded-full p-2 hover:cursor-pointer">
               {children}
          </div>
     );
}

export default IconBg;
